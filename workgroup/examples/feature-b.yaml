name: feature-b

worktree:
  branch: wg/feature-b

agents:
  - id: planner
    role: planner
  - id: executor
    role: executor

edges:
  - planner -> executor
  - executor -> planner

layout: even-horizontal
